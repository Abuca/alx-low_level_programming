#include "holberton.h"
/**
 * _strlen - check the code for Holberton School students.
 * @s: an integer
 * Return: the number of string
 */
int _strlen(char *s)
{
    int count = strlen(s);
    return(count);
}

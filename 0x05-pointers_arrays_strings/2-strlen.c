#include "holberton.h"
/**
 * _strlen - check the code for ALX School students.
 * @s: an integer
 * Return: Length of string.
 */
int _strlen(char *s)
{
int count;
count = _strlen(s);
return (count);
}
